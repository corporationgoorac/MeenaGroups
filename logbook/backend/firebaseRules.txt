rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This wildcard targets all documents in your entire database
    match /{document=**} {
      // Allows read and write access only if the user is signed in
      allow read, write: if request.auth != null;
    }
  }
}
